Publish the stream to MediaMTX over RTSP-interleaved TCP instead of UDP. `rtspclientsink` offered UDP first, and that hop dropped datagrams whenever the node was busy — MediaMTX discarded every frame a lost packet landed in, so viewers saw decoding artifacts until the next keyframe while the playhead, the frame-gap counter and OBS's skip counters all stayed clean.
