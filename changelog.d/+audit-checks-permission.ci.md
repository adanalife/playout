The advisory scan can publish its findings as a check run. It was missing `checks: write`, so the create-check call 403'd, the action reported that it "seems to be executed from the forked repository" — it isn't; that is just what a permission 403 looks like from inside it — and printed the advisory report into the job log instead, well below the line that says the job failed.
