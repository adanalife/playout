Match prod-1 playout deploy units by glob in release-please's version-pin set, so every prod platform's image tag bumps in lockstep on release and new platforms are picked up without editing the config.
