Trimmed the CI surface: the post-merge `cdk8s-synth` workflow is gone (the `pr-gates` synth step already covers every change into main), `platforms-contract` runs on its daily schedule only, and the base-image mirror installs a pinned `crane` release binary instead of compiling it from source on every run.
