Tag OTLP metrics with `deployment.environment` set to the k8s namespace (`prod-1`/`stage-1`) to match the rest of the fleet, instead of the NATS env (`production`/`staging`). Playout's series now match the shared Grafana dashboards' and alert rules' env filter.
