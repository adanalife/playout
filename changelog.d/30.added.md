cdk8s: a `playout-<platform>` Service exposes the HTTP control surface on :8080 (the name tripbot's `VLC_SERVER_HOST` dials after cutover), and the Deployment gains liveness/readiness probes against `/health/live` and `/health/ready`.
