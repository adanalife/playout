New `playout_publish_gap_seconds` metric: the duration of the most recent publish gap (deploy handoff or error recovery), so blackout length is measurable from Grafana instead of eyeballed from the stream.
