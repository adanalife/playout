`seek` command verb: move the playhead by a signed duration (`delta_ms`), walking real clip durations across boundaries in either direction and wrapping moves longer than the corpus modulo its total length — the backend for duration-based `!skip`/`!back`.
